# =============================================================
# requirements.txt
# NFL Super Bowl Champion Predictor
# Last updated: April 2026 | Python: 3.11+
# =============================================================
# HOW TO USE:
#   pip install -r requirements.txt
#
# CRITICAL CHANGE FROM OLDER VERSIONS:
#   nfl_data_py was DEPRECATED on Sep 25, 2025.
#   We now use nflreadpy — the official nflverse replacement.
#   nflreadpy returns Polars DataFrames (not pandas), so polars
#   must also be installed. Use .to_pandas() to convert.
# =============================================================


# ── NFL Data (UPDATED — nfl_data_py is dead) ─────────────────
# Official replacement, install directly from GitHub:
nflreadpy @ git+https://github.com/nflverse/nflreadpy

# nflreadpy uses Polars as its native DataFrame format
polars>=0.20.31


# ── Core Data Science ─────────────────────────────────────────
# pandas: we still use it everywhere downstream (sklearn, xgboost all expect pandas/numpy)
pandas>=2.2.2

# numpy: array math, Monte Carlo vectorization
numpy>=1.26.4

# scipy: stats utilities (logistic curves, calibration checks)
scipy>=1.13.0


# ── Machine Learning ──────────────────────────────────────────
# xgboost 2.x: major version — API changed from 1.x
# Key change: no more use_label_encoder param, new device= param for GPU
xgboost>=2.1.1

# scikit-learn: pipelines, TimeSeriesSplit CV, CalibratedClassifierCV
scikit-learn>=1.4.2

# shap: SHAP TreeExplainer for per-game feature attribution
# v0.45+ required for correct TreeExplainer output with xgboost 2.x
shap>=0.45.1


# ── Model Evaluation ──────────────────────────────────────────
# properscoring: Brier score and Ranked Probability Score vs Vegas odds
properscoring>=0.1.0


# ── Streamlit App ─────────────────────────────────────────────
# streamlit 1.35+ needed for st.cache_data with proper TTL + hash support
streamlit>=1.35.0

# plotly: interactive charts (win probability bars, EPA line charts, SHAP waterfall)
plotly>=5.20.0

# matplotlib: static fallback charts
matplotlib>=3.8.4


# ── Database & Auth ───────────────────────────────────────────
# bcrypt: password hashing for user accounts in the leaderboard
# Never store plain-text passwords — bcrypt adds a salt automatically
bcrypt>=4.1.3


# ── Utilities ─────────────────────────────────────────────────
# python-dotenv: loads .env file (Kaggle API key, secret keys, etc.)
python-dotenv>=1.0.1

# requests: fallback ESPN scraping if nflreadpy data is unavailable
requests>=2.32.3

# tqdm: progress bars during Monte Carlo simulation (10,000 runs)
tqdm>=4.66.4


# ── Dev / Testing ─────────────────────────────────────────────
# pytest: unit test runner
pytest>=8.2.0

# black: code formatter (keep style consistent across team)
black>=24.4.2